#include "main.h"

int main(void)
{
	int i = 123;
	char c = 'L';
	char *str = "we are great at what we do";

	printf("I can tell that %i is a num, %c is a char and %s\n", i, c, str);
	printf("It's also incredible that we can print \% using \\\n");
	if(printf != 0)
		return (1);
}
